import mongoose from "mongoose";

connectDB = async () => {
    try {
        connectionInstance = 
        
    } catch (error) {
        console.log("Erroe cOnnecting database",error);
        
    }
}