import mongoose from "mongoose";

connectDB = async () => {
    try {
        connectionInstance = await 
        
    } catch (error) {
        console.log("Erroe cOnnecting database",error);
        
    }
}